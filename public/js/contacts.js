contacts = {
	
	sendQuestion(formId){
        //let data = this.readForm(formId); 
        //this.send(data);
        alert(formId);
	},

	readForm(formId){
		alert('read data' + formId);
	},
    
	send(data){
        // ajax post
        // show response
	}
   
}